import React from 'react';

export default React.createClass( {

    render() {
        return (
            <div id="recipe">
                <h1>Recipe</h1>
            </div>
        );
    }

} );